kind: Service
apiVersion: v1
metadata:
  name: servicio-tutorial-server
spec:
  type: LoadBalancer
  selector:
    app: tutorial-server
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080